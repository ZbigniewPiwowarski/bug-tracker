pipeline {
    agent any

    stages {
        stage('Hello') {
            agent{
                image 'snakee/golang-junit:1.21'
                reuseNode true
            }
            steps {
                dir ('bugtracker-backend')
                sh 'go test -v ./...'
            }
        }
    }
}
